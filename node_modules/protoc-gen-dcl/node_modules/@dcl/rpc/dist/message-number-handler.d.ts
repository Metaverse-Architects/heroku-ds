import { BinaryReader } from "google-protobuf";
import { Transport } from ".";
export declare type SendableMessage = {
    setMessageId(number: number): void;
    serializeBinary(): Uint8Array;
    toObject(): any;
};
export declare type MessageDispatcher = {
    transport: Transport;
    request(data: SendableMessage): Promise<BinaryReader>;
    addListener(messageId: number, handler: (reader: BinaryReader) => void): void;
    removeListener(messageId: number): void;
};
export declare function messageNumberHandler(transport: Transport): MessageDispatcher;
//# sourceMappingURL=message-number-handler.d.ts.map