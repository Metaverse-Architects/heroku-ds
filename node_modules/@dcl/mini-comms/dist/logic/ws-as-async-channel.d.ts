import { AsyncQueue } from '@dcl/rpc/dist/push-channel';
import { WebSocket } from 'ws';
import { WsPacket } from '../proto/ws-comms-rfc-5';
export declare function wsAsAsyncChannel(socket: WebSocket): AsyncQueue<WsPacket> & {
    yield(timeoutMs: number, error?: string): Promise<WsPacket>;
};
