"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.printConsole = void 0;
const helpers_1 = require("./helpers");
/**
 * @internal
 */
const printConsole = (components, kind, loggerName, message, extra) => {
    (0, helpers_1.incrementMetric)(components, loggerName, kind);
    // logs should go to the stderr to be properly forwarded in some environments
    const extraLine = extra ? "\t" + JSON.stringify(extra) : "";
    return process.stderr.write(new Date().toISOString() + " [" + kind + "] (" + loggerName + "): " + message + extraLine + "\n");
};
exports.printConsole = printConsole;
//# sourceMappingURL=console-printer.js.map