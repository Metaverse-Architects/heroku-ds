import { IBaseComponent } from "@well-known-components/interfaces";
import { natsComponent, INatsComponent, Subscription } from "./types";
export { createLocalNatsComponent } from "./test-component";
export { encodeJson, decodeJson } from "./codecs";
export { Subscription };
/**
 * Create a NATS component (https://nats.io/)
 * Connect to a NATS node on start(), via the env variable "NATS_URL"
 * @public
 */
export declare function createNatsComponent(components: natsComponent.NeededComponents): Promise<INatsComponent & IBaseComponent>;
//# sourceMappingURL=index.d.ts.map